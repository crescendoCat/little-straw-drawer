# Copy to `.env.local` (git-ignored) and fill in your own values.

# OAuth 2.0 Client ID (application type "Web application") used by the
# "Back up to Google Drive" feature. Leave empty to disable the feature; the
# cloud icon then shows "not configured". See README > Google Drive backup.
VITE_GOOGLE_CLIENT_ID=
